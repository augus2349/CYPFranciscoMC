BASE = float(input("ingrese el valor de la base "))
ALTU = float(input("ingrese el valor de la altura "))
SUP = (BASE * ALTU) / 2
print(f"la superficie del triangulo es: { SUP } dado que la atura es { ALTU } U. y la base es { BASE } U.")
